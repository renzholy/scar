query listBlocks($after: String) {
  blocks(after: $after, first: 100) {
    pageInfo {
      hasNextPage
    }
    edges {
      cursor
      node {
        id
        timestamp
        height
        previous
      }
    }
  }
}
